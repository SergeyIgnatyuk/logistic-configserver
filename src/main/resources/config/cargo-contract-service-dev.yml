spring:
  kafka:
    bootstrap-servers: logistic-kafka:9093
  datasource:
    url: jdbc:postgresql://logistic-storage:5432/logistic
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
