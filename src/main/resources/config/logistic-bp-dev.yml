spring:
  datasource:
    url: jdbc:postgresql://camunda-storage:5434/camunda
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
camunda:
  bpm:
    admin-user:
      id: user
      password: user
